// Commenting out the unused FileInfo type
/*
export interface FileInfo {
  name: string;
  path: string;
  size: number;
  lastModified: number;
  isBinary: boolean;
  tokenCount: number; 
  excludedByDefault: boolean;
  excluded: boolean;
  isSkipped: boolean; 
}
*/

export {}; // Keep this if the file becomes empty 